<!DOCTYPE html>
<html lang="en">
  <%- include('./head', { pageTitle }) %>
  <body>
    <div class="app-shell">
      <%- include('./nav', { activeNav: typeof activeNav !== 'undefined' ? activeNav : '' }) %>

      <div class="app-main">
        <%- include('./topbar', { topbarTitle: typeof topbarTitle !== 'undefined' ? topbarTitle : pageTitle }) %>

        <main class="content-shell">